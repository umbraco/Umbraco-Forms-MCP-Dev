# Environment files — never commit credentials
.env
.dev.vars

# Cloudflare Worker local state
.wrangler/

# Build output
dist/

# Umbraco instance (contains credentials, DB files, build artifacts)
demo-site/

# Dependencies
node_modules/

# Written by jest-failure-reporter.ts on any test failure; auto-deleted on a clean run
test-failures.log

# Left behind only if run-evals-isolated.mjs is killed before it can restore .mcp.json
.mcp.json.evals-bak

# OS
.DS_Store
Thumbs.db

# Claude Code local/session state (not shared project config)
.claude/settings.local.json
.claude/kanban-state.json
.claude/scheduled_tasks.lock